# 2026-09-05 PR 검토 요약

## 현재 상태

전체 코드 재검토 후 확인한 기능 결함을 수정하고 회귀 테스트를 추가했다. 마지막 길 안내 수정까지 포함한 최종 재검증에서 655개를 실행해 654개 통과, 실기기 전용 1개 skip, 실패 0개를 확인했다. Release 무서명 빌드·기기 archive·정적 분석도 통과했다. 완성도 98%나 실기기 출시 준비 완료를 주장하지 않는다.

PR의 기준 `main`은 초기 프로젝트(`7133e94`)이므로, 이전에 작업 폴더에 쌓인 공간 인식·저장·조회·배치·안내 구현과 이번 재검토 수정이 함께 포함된다. 서명 정보·시험 산출물은 포함하지 않는다.

## 주요 변경

| 영역 | 반영 내용 |
| --- | --- |
| 앱 구조·생명주기 | 의존성 조립, 화면 진입, 시작·중단·삭제 경계를 분리했다. 삭제 전에 취소된 이전 세대의 작업까지 기다리고, 늦은 비동기 결과가 새 상태를 덮어쓰지 않도록 했다. |
| 저장·개인정보 | 공통 저장 디렉터리 정책으로 파일 보호와 OS 백업 제외를 적용했다. 기존 저장 루트, 읽기·쓰기·격리·삭제 후 재생성에도 적용하며, 과거 백업 사본은 앱이 삭제할 수 없다는 안내를 유지한다. |
| 공간·조회 | 관계 저장의 중복 방지 이력을 제한된 창으로 관리하고 오래된 재전송은 revision으로 거부한다. 물체 이동 시 경계 상자를 갱신하며, 프레임 대기 버퍼 해제와 조회·마지막 위치·배치 명령 분리를 보완했다. |
| 가구 배치 | 후보 전체와 여유 공간이 실제 바닥 다각형 및 같은 높이의 분류된 바닥 삼각형으로 덮이는지 확인한다. 오목 영역·구멍·분리된 바닥·천장·잘린 메시를 안전한 직사각형으로 간주하지 않는다. 관측된 국소 영역은 활용하되 통로 증거 부족과 실제 부적합을 구분한다. 문 계산 비용과 취소도 제한한다. |
| 길 안내 | 같은 세션의 평가 시각으로 지도·현재 위치를 확인하고 실제 출발점과 목적지 접근 구간의 벽·장애물·관측 범위를 검증한다. 대상의 실제 장애물만 제한적으로 예외 처리하며, 도달 가능한 접근 면을 우선 선택한다. 반대편 후보 때문에 잘못 도달 불가로 판단하던 사례까지 수정·재시험했다. |
| 수치·복합 질의 | 깊이 이미지 크기와 극단적인 샘플 반경의 정수 오버플로, 음수 초점 거리, 투영 후 비정상 좌표를 거부한다. 의미 대상이 세 개 이상인 관계 질문은 일부 대상을 버리고 단정하지 않고 모호함으로 반환한다. |
| 인식·시간 정보 | 32프레임 증거 창이 이동한 뒤에도 재식별 요청이 계속 처리되도록 현재 창의 최초 관측 시간을 사용한다. 기존 영구 ID의 원래 최초 관측 시간은 보존한다. 복원 시 업데이트 시계와 부재 증거 시계가 각각 모두 존재하는지 검증하고 가중치 합계 오버플로를 거부한다. |
| 부재 판단 | 단순히 카메라 시야 안에 있다는 이유만으로 물체가 사라졌다고 판단하지 않는다. 같은 프레임의 고신뢰 원시 깊이가 저장 위치보다 충분히 뒤에 있어야 한다. 가림, 같은 표면, 누락·비정상·저신뢰 깊이와 평활 깊이만 있는 경우는 부재 증거에서 제외한다. |
| 연관 시도 이력 | 모든 재생 단계가 `deferDecision`인 오래된 시도만 새 시도와 원자적으로 교체한다. 영구 watermark로 퇴역 시도의 늦은 재삽입을 막고, 남은 시도의 정확한 재시도·추가 관측·오래된 쓰기 거부를 유지한다. 기기 시계가 뒤로 움직여도 새 시도의 생성 순서는 보존한다. |
| 검증 경로 | 공통 모델 무결성 확인, 실행별 독립 결과·빌드 경로, 회귀 테스트를 유지한다. 시뮬레이터의 실제 파일 보호 속성 검증 한계는 설정 호출 테스트와 실기기 전용 테스트로 구분했다. |
| 프로젝트 재현성 | XcodeGen 재생성 시 수동 변경된 서명·언어 설정이 달라지는 문제를 수정했다. 리소스를 명시적으로 한 번만 포함하여 한국어 지역을 발견하게 하고, 중복 그룹 이름의 비결정적 정렬도 제거했다. 서명 identity는 승인된 로컬 설정을 상속하며 빈 값이나 개인 팀 정보를 저장소에 넣지 않는다. |

연관 시도에 지도 생성·기존 지도 연결·병합 제안이 한 번이라도 포함되면, 마지막 결정이 보류여도 미처리 변경으로 간주해 보존한다. 영구 적용 완료를 확인할 계약이 없으므로 임의로 정리하지 않는다. 따라서 보존된 변경 제안만으로 용량이 찬 경우에는 기존 용량 오류가 남는다. 단순히 한도를 늘리거나 미처리 제안을 잊도록 만든 수정이 아니다.

## 보안 검토와 교차 검토

- 정적 보안 스캔 `77634e74-af83-4e6e-91dd-d898500de0c9`에서 대상 133개 파일 전체를 검토했으며, 새로 보고할 보안 취약점은 확인하지 못했다.
- 검토 중 소스를 동결했고 스캔을 봉인한 뒤 기능 결함을 수정했다. 따라서 이 스캔 결과를 이후 변경본 자체의 스캔 결과로 바꿔 표기하지 않는다.
- 이후 변경은 담당자를 바꿔 독립 교차 검토했다. 깊이 기반 부재 판단, 기록 회전과 미처리 변경 보존, 증거 창, 복원 시계, 수치 검증을 다시 대조했다. 최종 런타임 검증은 아래 별도 항목으로 관리한다.
- 이전에 확인한 OS 백업 경계 문제의 보호 정책은 구현 후 집중 시험으로 재확인했다. 이는 실제 OS 백업 추출 시험이나 운영체제 자체의 보안 검증을 의미하지 않는다.

스캔 도구가 반환한 **누적 사용량은 15,118,792 tokens**다. 이번 대화 구간만의 사용량이나 코드 품질·제품 완성도 수치가 아니다.

## 검증 결과

| 항목 | 확인 상태 |
| --- | --- |
| 저장 보호·삭제·생명주기 집중 시험 | 46개 실행: 45개 통과, 실기기 전용 1개 skip, 실패 0개. 이전 문서의 연결 단절로 미완료였던 재시험을 이번에 완료했다. |
| 첫 전체 실행 | 코어 XCTest 305개 + Swift Testing 9개 통과. iOS 340개 실행: 339개 통과, 실기기 전용 1개 skip, 실패 0개. Release 무서명 빌드·기기 archive·분석도 성공. 마지막 길 안내 변경의 최종 통과 증거로 사용하지 않는다. |
| 최종 코어 | 새 scratch directory에서 XCTest 306개 + Swift Testing 9개, 315개 통과. |
| 최종 iOS·UI | 새 DerivedData에서 단위 테스트 329개(328 통과, 실기기 전용 1개 skip), UI 테스트 11개 통과. 합계 340개 실행, 실패 0개. |
| 최종 Release·분석 | Simulator Release build, generic iOS unsigned archive, Debug analyze 성공. 모델 SHA-256, 컴파일된 모델과 한국어 리소스 포함, Release에서 7개 Debug 실행 토큰 부재 확인. 서명된 배포 archive는 아니다. |
| 마지막 길 안내 변경 재검증 | `testDestinationApproachChoosesReachableSideOfOccupiedTarget`을 포함한 최종 전체 코어 실행 통과. 이전 빌드 캐시를 공유하지 않는 최종 scratch/DerivedData로 다시 컴파일했다. |
| 프로젝트·정적 검사 | XcodeGen 2.46.0 재생성 전후 동일, 최종 빌드 후 재생성해도 157개 소스 해시 동일. Swift 서식 lint, 셸 문법, 문자열 JSON, privacy plist, 전체 staged diff whitespace 검사 통과. 실행용 셸 스크립트는 Git executable mode로 기록. |
| 최종 검증 소스 및 로컬/원격/index 일치 | 소스·테스트·리소스·빌드 설정 157개 각각의 SHA-256 일치. 경로별 해시 목록의 SHA-256은 `564567fde8f36f80e36b17541988c1bfb4b0bc488ee084a559a85ca3b7ce3114`. Git index의 파일 내용도 동일함을 확인했다. 문서와 Git 파일 모드는 이 소스 내용 목록에 포함하지 않는다. |
| 수정본 실기기 설치·현장 검수 | 미검증. 시뮬레이터 통과로 대체하지 않는다. |

도구 환경은 Xcode 26.6(17F113), iPhone 17 Pro Simulator/iOS 26.5다. 검증은 원격 빌드 복사본에서 단계별 명령을 실행했다. 복사본에 `.git`이 없으므로 `verify-ios.sh` 자체의 clean-checkout Git 단계 통과라고 주장하지 않는다. XcodeGen 출력과 전송 파일의 해시는 별도로 대조했다. Xcode의 AppIntents 의존성 없음에 따른 metadata extraction 안내 경고는 남아 있으며, 앱에 AppIntents 기능이 구현됐다는 의미는 아니다.

최종 원격 결과 폴더는 `/Users/dlfkd/CodexBuild/vispace-phase1-9d4e2d741dbd4474bd25fffb4ab1a850/TestResults/PR-2026-09-05/final.o1JKcq`다. `Tests.xcresult`, 단계별 로그와 소스 대조 기록을 보존했다. 로컬 `TestResults/PR-2026-09-05`에는 내려받은 로그, `source-manifest.json`, `source-manifest.sha256`을 보존하며 PR에는 빌드 산출물이나 개인 서명 설정을 넣지 않는다.

새 회귀 테스트에는 40프레임 연속 재식별, 업데이트·부재 시계 누락 복원, 가중치 합계 오버플로, 가림·깊이 누락·잘못된 깊이·낮은 신뢰도, 기록 한도 초과 회전·재시작·늦은 재삽입·취소·과거 변경 제안 보존·역행 시계를 포함했다.

## 남은 한계와 이전 기록

가구 판단은 관측된 공간의 기하학적 적합성 평가다. 취향·인테리어 스타일을 이해하는 자유 대화형 AI 추천이나 실제 방에서의 정확도를 검증했다고 표현하지 않는다. 실제 기기의 재인식·이동 오차, 가림·반사 표면, 경로 안전성, 장시간 발열·메모리, 비LiDAR 동작과 파일 보호 속성은 [실기기 검수표](DEVICE_ACCEPTANCE.md)로 별도 확인해야 한다.

[이전 코드 리뷰](CODE_REVIEW_2026-09-05.md)와 [이전 구현·검증 기록](VERIFICATION_2026-09-05.md)은 당시 소스와 실행 결과를 보존한 역사적 기록이다. 이전 테스트 수를 현재 변경본의 통과 수에 합산하지 않으며, 과거 연결 단절·검증 대기 상태와 이번 재시험 결과를 구분한다.
